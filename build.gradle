subprojects {
    apply plugin: 'java'
    apply plugin: 'application'

    version = '1.0.0'

    repositories {
        mavenCentral()
    }

    dependencies {
       testImplementation 'junit:junit:4.13.2'
    }
}